#include "../../cub3d.h"

void	get_direction(t_player *player)
{
	double	fov_in_radian;
	double	plan_camera_length;

	fov_in_radian = FOV_DEGREE * (PI / 180);
	player->camera.dir_x = sin
}

int	**get_raydirs(t_game *game)
{
	
}